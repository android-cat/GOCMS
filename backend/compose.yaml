services:
  backend:
    build: # ビルドに使うDockerファイルのパス
      context: .
      dockerfile: ./build/package/Dockerfile
    volumes: # マウントディレクトリ
      - ./:/app
    env_file: # .envファイル
      - ./build/package/.env
    ports:
      - 8080:8080
